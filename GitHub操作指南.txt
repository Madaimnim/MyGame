切換到專案的資料料夾
cd /d/Practice/TowerDefenseGame

把這個資料夾裡所有變更過的檔案（修改、新增、刪除）都加入 Git 暫存區（staging area）
git add .

建立一個 commit，訊息是「更新專案」，把暫存的檔案變更紀錄下來。
git commit -m "更新專案"

把本地的 main 分支推送到遠端的 origin（通常是 GitHub）。
git push origin main

等使用者按任意鍵才結束，避免執行完就直接關閉終端視窗。
read -p "按任意鍵繼續..."